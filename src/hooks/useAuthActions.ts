
// Este arquivo não é mais necessário - substituído pela integração com Supabase
// Mantido apenas para compatibilidade durante a transição
export const useAuthActions = () => {
  return {
    signupUser: () => {},
    loginUser: () => null,
    logoutUser: () => {},
  };
};
